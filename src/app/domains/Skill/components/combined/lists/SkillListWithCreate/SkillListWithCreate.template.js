import PropTypes from 'prop-types'

/**
 * @info SkillListWithCreate (05 Mar 2021) // CREATION DATE
 *
 * @comment SkillListWithCreate - React component.
 *
 * @since 05 Mar 2021 ( v.0.0.1 ) // LAST-EDIT DATE
 *
 * @return {ReactComponent}
 */

const SkillListWithCreate = (props) => {
  // [INTERFACES]
  /*
  code sample: 
  const { data } = props
  */

  // [ADDITIONAL_HOOKS]
  /*
  code sample: 
  const firestore = useFirestore()
  */

  // [COMPONENT_STATE_HOOKS]
  /*
  code sample:
  const singleton = useRef(true) // references also put here
  const [state, setState] = useState({})
  */

  // [HELPER_FUNCTIONS]

  // [COMPUTED_PROPERTIES]
  /* 
    code sample: 
    const userDisplayName = user.firstName + user.lastName
  */

  // [USE_EFFECTS]

  // [TEMPLATE]
  return <div>SkillListWithCreate</div>
}

// [PROPTYPES]
SkillListWithCreate.propTypes = {}

export default SkillListWithCreate
