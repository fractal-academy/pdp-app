import React from 'react'
import LevelModalWithForm from './LevelModalWithForm.template'

const metadata = {
  title: 'app/domains/Level/components/combined/modals/LevelModalWithForm',
  component: LevelModalWithForm
}
export default metadata

export const LevelModalWithFormStory = (args) => (
  <LevelModalWithForm {...args} />
)

LevelModalWithFormStory.args = {}
