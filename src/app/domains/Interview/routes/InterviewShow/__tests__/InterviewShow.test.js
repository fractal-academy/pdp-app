import React from 'react'
import { render } from '@testing-library/react'
import InterviewShow from '../InterviewShow.layout'

test('Render InterviewShow test.', () => {
  render(<InterviewShow />)
})
