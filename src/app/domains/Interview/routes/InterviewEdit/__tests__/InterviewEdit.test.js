import React from 'react'
import { render } from '@testing-library/react'
import InterviewEdit from '../InterviewEdit.layout'

test('Render InterviewEdit test.', () => {
  render(<InterviewEdit />)
})
