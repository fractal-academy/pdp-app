import React from 'react'
import { render } from '@testing-library/react'
import InterviewCreate from '../InterviewCreate.layout'

test('Render InterviewCreate test.', () => {
  render(<InterviewCreate />)
})
