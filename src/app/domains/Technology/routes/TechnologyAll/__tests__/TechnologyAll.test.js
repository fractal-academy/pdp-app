import React from 'react'
import { render } from '@testing-library/react'
import TechnologyAll from '../TechnologyAll.layout'

test('Render TechnologyAll test.', () => {
  render(<TechnologyAll />)
})
