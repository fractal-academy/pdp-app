import React from 'react'
import { render } from '@testing-library/react'
import SpecialityCreate from '../SpecialityCreate.layout'

test('Render SpecialityCreate test.', () => {
  render(<SpecialityCreate />)
})
