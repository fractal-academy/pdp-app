import React from 'react'
import { render } from '@testing-library/react'
import CompetenceAll from '../CompetenceAll.layout'

test('Render CompetenceAll test.', () => {
  render(<CompetenceAll />)
})
