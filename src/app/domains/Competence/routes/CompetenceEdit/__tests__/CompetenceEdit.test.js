import React from 'react'
import { render } from '@testing-library/react'
import CompetenceEdit from '../CompetenceEdit.layout'

test('Render CompetenceEdit test.', () => {
  render(<CompetenceEdit />)
})
