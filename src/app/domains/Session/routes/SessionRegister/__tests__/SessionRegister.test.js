import React from 'react'
import { render } from '@testing-library/react'
import SessionRegister from '../SessionRegister.layout'

test('Render SessionRegister test.', () => {
  render(<SessionRegister />)
})
