import React from 'react'
import { render } from '@testing-library/react'
import NotificationSimpleList from '../NotificationSimpleList.template'

test('Render NotificationSimpleList test.', () => {
  render(<NotificationSimpleList />)
})
